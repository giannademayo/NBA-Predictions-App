# Notebook reference

`original_nba.ipynb` is included as a reference copy of the exploratory notebook. The Streamlit app does not execute it directly; the reusable code lives in `src/pipeline.py` and `src/prediction.py`.
